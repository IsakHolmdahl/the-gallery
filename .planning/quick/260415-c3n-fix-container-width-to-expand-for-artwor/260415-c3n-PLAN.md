---
phase: quick-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/gallery-content.tsx]
autonomous: true
requirements: [BUG-01]
user_setup: []

must_haves:
  truths:
    - "Container expands to accommodate 70vw artwork when isArtworkShowing is true"
    - "Container stays at reasonable max width when showing form (isArtworkShowing false)"
  artifacts:
    - path: "src/components/gallery-content.tsx"
      provides: "Conditional container width based on artwork state"
      contains: "className with ternary operator"
  key_links:
    - from: "src/components/gallery-content.tsx"
      to: "PromptForm component"
      via: "isArtworkShowing state"
      pattern: "isArtworkShowing.*w-full.*max-w-xl"
---

<objective>
Fix container width clipping artwork by making it expand when artwork is shown, but stay reasonable when showing the form.

Purpose: The current fixed 20vw width clips the 70vw artwork display. Need conditional width that expands for artwork and stays constrained for the form.
Output: Modified gallery-content.tsx with conditional className based on isArtworkShowing state.
</objective>

<execution_context>
@/Users/isakholmdahl/Git/art-teacher/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/isakholmdahl/Git/art-teacher/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./AGENTS.md
@src/components/gallery-content.tsx

Current state: GalleryContent component uses `max-w-full w-[20vw]` at line 40, which constrains container width to 20 viewport widths. When artwork is displayed (70vw), this clips the artwork.

The `isArtworkShowing` state is already available (line 13) and tracks whether artwork is displayed.
</context>

<interfaces>
From src/components/gallery-content.tsx:
```typescript
export function GalleryContent({ artworkSize }: GalleryContentProps)
// State: const [isArtworkShowing, setIsArtworkShowing] = useState(false);
// Current problematic line: <div className="max-w-full w-[20vw]">
```
</interfaces>

<tasks>
<task type="auto" tdd="false">
  <name>Task 1: Update container width to be conditional</name>
  <files>src/components/gallery-content.tsx</files>
  <action>Replace line 40's className "max-w-full w-[20vw]" with conditional logic:
- When isArtworkShowing is true: className="w-full" (no max constraint, expands to accommodate 70vw artwork)
- When isArtworkShowing is false: className="w-full max-w-xl" (reasonable form size)

Use template literal with ternary operator:
className={`w-full ${isArtworkShowing ? '' : 'max-w-xl'}`}

This maintains the w-full base while conditionally adding max-w-xl only when artwork is not showing.

Verify the change by checking the file contains the new className pattern.</action>
  <verify>
    <automated>grep -n "className={\`w-full \${isArtworkShowing" src/components/gallery-content.tsx</automated>
  </verify>
  <done>
    Container width is conditional: expands when artwork shows (w-full), constrained when form shows (w-full max-w-xl). No more clipping of 70vw artwork.
  </done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client | No external trust boundaries - pure frontend CSS change |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | N/A | gallery-content.tsx | accept | No security impact - UI styling only |
</threat_model>

<verification>
1. Check line 40 in gallery-content.tsx contains conditional className
2. Verify isArtworkShowing state is used in the condition
3. Ensure the fix follows the project's React/TypeScript patterns
</verification>

<success_criteria>
- [ ] Container expands to full width when isArtworkShowing is true
- [ ] Container has max-w-xl when isArtworkShowing is false
- [ ] No TypeScript errors in the component
- [ ] Existing functionality preserved (form still works, artwork display works)
</success_criteria>

<output>
After completion, create `.planning/quick/260415-c3n-fix-container-width-to-expand-for-artwor/260415-c3n-SUMMARY.md`
</output>