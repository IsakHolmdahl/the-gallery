# Quick Task 260420-hr1: Fix Vercel deployment build failure

## Plan

### Task 1: Exclude vitest config files from Next.js TypeScript compilation

**Files:** `tsconfig.json`
**Action:** Add `vitest.config.ts` and `vitest.setup.ts` to the `exclude` array in `tsconfig.json`
**Verify:** `npm run build` completes successfully without vitest/config module error
**Done:** Build passes locally and vitest files are excluded from TypeScript compilation
